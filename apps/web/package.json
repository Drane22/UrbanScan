{
  "name": "@every-qrcode/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @every-qrcode/core build && pnpm --filter @every-qrcode/renderer-webgpu build && pnpm --filter @every-qrcode/react build && pnpm --filter @every-qrcode/web-component build && tsc --noEmit -p ../../tsconfig.json && vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@every-qrcode/core": "workspace:*",
    "@every-qrcode/react": "workspace:*",
    "@every-qrcode/renderer-webgpu": "workspace:*",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.1.0",
    "vite": "8.2.2"
  }
}
